{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$comment": "LLM: Keep in sync with entity.go Code struct",
  "title": "Code",
  "type": "object",
  "required": ["id", "healthy", "version", "project_id", "slug", "color", "definition"],
  "properties": {
    "id": { "type": "string" },
    "healthy": { "type": "boolean" },
    "version": { "type": "integer" },
    "project_id": { "type": "string" },
    "slug": {
      "type": "string",
      "minLength": 3,
      "maxLength": 100,
      "pattern": "^[a-z]+(-[a-z]+)*:[a-z]+(-[a-z]+)*$",
      "description": "e.g. emotion:anxiety, theme:power-dynamics"
    },
    "color": {
      "type": "string",
      "enum": ["gray", "mauve", "slate", "sage", "olive", "sand", "tomato", "red", "ruby", "crimson", "pink", "plum", "purple", "violet", "iris", "indigo", "blue", "cyan", "teal", "jade", "green", "grass", "bronze", "gold", "brown", "orange", "amber", "yellow", "lime", "mint", "sky"],
      "description": "Radix base color"
    },
    "definition": {
      "type": "string",
      "description": "Core definition: What does this code mean? 1-2 paragraphs max."
    },
    "inclusion_criteria": {
      "type": "string",
      "description": "When to apply: Clear criteria for when this code should be used. 1-2 paragraphs max."
    },
    "exclusion_criteria": {
      "type": "string",
      "description": "When NOT to apply: What might look similar but shouldn't be coded this way? 1 paragraph max."
    },
    "examples": {
      "type": "array",
      "items": { "type": "string" },
      "description": "3-5 representative text snippets that exemplify this code."
    },
    "counter_examples": {
      "type": "array",
      "items": { "type": "string" },
      "description": "2-3 snippets that might seem like they fit but don't."
    },
    "pinned": { "type": "boolean" }
  }
}
